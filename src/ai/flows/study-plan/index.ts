// This file is intentionally left blank.
// The primary logic has been moved to the `/api/generate-plan` route
// to support streaming responses, which cannot be done with a standard
// server action flow file. This directory is kept for schema, prompt, and
// utility definitions that are shared with the new API route.
